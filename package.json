{
  "name": "ai-contractor-assessment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html styles.css app.js dist/ && cp -R public dist/public"
  }
}
